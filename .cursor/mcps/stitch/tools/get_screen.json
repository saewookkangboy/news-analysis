{
  "name": "get_screen",
  "description": "Gets metadata for a specific screen.",
  "arguments": {
    "type": "object",
    "properties": {
      "screen_id": {
        "type": "string",
        "description": "The ID of the screen to retrieve"
      },
      "project_id": {
        "type": "string",
        "description": "The ID of the project containing the screen"
      }
    },
    "required": ["screen_id", "project_id"]
  }
}
