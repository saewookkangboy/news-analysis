{
  "name": "fetch_screen_code",
  "description": "Downloads the raw HTML/Frontend code of a screen.",
  "arguments": {
    "type": "object",
    "properties": {
      "screen_id": {
        "type": "string",
        "description": "The ID of the screen to fetch code from"
      },
      "project_id": {
        "type": "string",
        "description": "The ID of the project containing the screen"
      }
    },
    "required": ["screen_id", "project_id"]
  }
}
