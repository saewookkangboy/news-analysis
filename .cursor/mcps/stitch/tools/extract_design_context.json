{
  "name": "extract_design_context",
  "description": "Scans a screen to extract 'Design DNA' (Fonts, Colors, Layouts). Use this to keep style consistent.",
  "arguments": {
    "type": "object",
    "properties": {
      "screen_id": {
        "type": "string",
        "description": "The ID of the screen to extract design context from"
      },
      "project_id": {
        "type": "string",
        "description": "The ID of the project containing the screen"
      }
    },
    "required": ["screen_id", "project_id"]
  }
}
