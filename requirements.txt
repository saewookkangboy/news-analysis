# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
mangum==0.17.0
starlette==0.27.0

# HTTP & Web Scraping
requests==2.31.0
beautifulsoup4==4.12.2
lxml==4.9.3

# Data Processing
# pandas==2.1.3  # Vercel 배포 시 크기 제한으로 주석 처리 (필요시 활성화)
# numpy==1.26.2  # Vercel 배포 시 크기 제한으로 주석 처리 (필요시 활성화)

# NLP & Korean Language Processing
# sentence-transformers==2.2.2  # Vercel 배포 시 크기 제한으로 제거 (모델 파일이 매우 큼)

# AI API
openai>=1.12.0  # 최신 버전 사용 (proxies 오류 수정)
google-generativeai==0.3.0  # 기존 방식 지원
google-genai>=0.2.0  # 새로운 Gemini API 클라이언트 (선택사항)

# Utilities
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Development (optional for Vercel)
pytest==7.4.3
pytest-asyncio==0.21.1
